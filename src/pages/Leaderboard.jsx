// toggle between NAME and NATIONALITY

// NAME: toggle between points and score (which is skewed by players who only did one challenge)
// NATIONALITY: toggle between SUM(points) and SUM(score)

import React from 'react'

function Leaderboard() {
  
  return (
    <div>Leaderboard</div>
  )
}

export default Leaderboard